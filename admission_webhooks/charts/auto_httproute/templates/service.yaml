apiVersion: v1
kind: Service
metadata:
  name: {{ include "auto_httproute.fullname" . }}
  labels:
    {{- include "auto_httproute.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "auto_httproute.selectorLabels" . | nindent 4 }}
