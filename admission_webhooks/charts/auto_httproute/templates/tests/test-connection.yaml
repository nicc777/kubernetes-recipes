apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "auto_httproute.fullname" . }}-test-connection"
  labels:
    {{- include "auto_httproute.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "auto_httproute.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
